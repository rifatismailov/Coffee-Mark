<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" />  <!-- Заливка фону -->
    <stroke
        android:color="@android:color/darker_gray"
    android:width="2dp" />  <!-- Товщина лінії -->
    <padding
        android:left="0dp"
        android:top="2dp"
        android:right="0dp"
        android:bottom="2dp" />  <!-- Відступи для лінії -->
</shape>
